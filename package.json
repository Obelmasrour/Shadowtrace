{
  "name": "shadowtrace-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd main && npm start",
    "client": "cd shadowtrace-ui/project && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
